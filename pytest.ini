[pytest]
testpaths = tests
# The ODL package ships a pytest plugin that is incompatible with recent pytest versions.
addopts = -p no:odl_plugins -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
